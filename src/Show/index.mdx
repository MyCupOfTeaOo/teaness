---
name: Show(显示)
route: /Show
menu: Display
---

import { Playground, Props } from 'docz';
import { Button } from 'antd';
import 'antd/dist/antd.css';
import Show, { Decision } from '.';

# Show(显示)

控制 children 是否显示

## 基本用法(Basic usage)

<Playground>
  <Show actual={0} expect={0}>
    显示
  </Show>
  <Show actual={0} expect={1}>
    不显示
  </Show>
</Playground>

## 属性(Properties)

<Props of={Show} />

# Decision(匹配显示)

根据 Decision.Case 组件的匹配显示

## 基本用法(Basic usage)

<Playground>
  <Decision actual={0}>
    <Decision.Case expect={0}>显示</Decision.Case>
    <Decision.Case expect={1}>不显示</Decision.Case>
    <div>
      <Decision.Case expect={0}>显示2</Decision.Case>
      <Decision.Case expect={1}>不显示2</Decision.Case>
    </div>
  </Decision>
</Playground>

## 属性(Properties)

<Props of={Decision} />
