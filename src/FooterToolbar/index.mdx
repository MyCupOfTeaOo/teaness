---
name: FooterToolbar(底部工具栏)
route: /FooterToolbar
menu: components
---

import { Playground, Props } from 'docz';
import { Button } from 'antd';
import 'antd/dist/antd.css';
import './mdx.scss';
import FooterToolbar from '.';
import IFramePlayground from 'docz-iframe-playground';

# FooterToolbar(底部工具栏)

抄的 antd pro 的

需要menu 收缩触发 resize

## 基本用法(Basic usage)

颜色其实是白色 但是被我翻转了颜色 方便查看轮廓

<Playground>
  <div style={{padding: 20, backgroundColor: "#e5e5e5"}}>
    <IFramePlayground>
      <div className="content">
        <FooterToolbar>
          <Button>提交</Button>
        </FooterToolbar>
      </div>
    </IFramePlayground>
  </div>
</Playground>

## 属性(Properties)

<Props of={FooterToolbar} />
