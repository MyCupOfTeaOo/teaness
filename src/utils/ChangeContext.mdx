---
name: ChangeContext(变更上下文)
route: /change-context
menu: utils
---

import { useState, useEffect } from 'react';
import { Playground, Props } from 'docz';
import ChangeContext from './ChangeContext';

# ChangeContext(变更上下文)

## 基本用法(Basic usage)

```jsx
<ChangeContext context={你的context} data={新的上下文}>
  ...
</ChangeContext>
```

## 属性(Properties)

<Props of={ChangeContext} />
